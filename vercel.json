{
   "version": 2,
   "name": "phonebook-api",
   "builds": [
      { "src": "app.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/api/contacts(.*)", "dest": "/app.js" },
      { "src": "/api/users(.*)", "dest": "/app.js" },
      { "src": "/users(.*)", "dest": "/app.js" }
   ]
}